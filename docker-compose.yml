version: '3.8'
services:
  db:
    image: postgres:15
    container_name: smap_db-1
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Yatesh1234? 
      POSTGRES_DB: stockmarket_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: