version: '3.8'
services:
  smap_db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Yatesh1234?
      POSTGRES_DB: stockmarket_db
    ports:
      - "5432:5432"
    volumes:
      - smap_data:/var/lib/postgresql/data

volumes:
  smap_data: